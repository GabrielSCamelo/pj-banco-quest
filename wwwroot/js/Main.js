// wwwroot/js/app.js
var app = angular.module('PJ_DB_QUEST', []);

// Você pode adicionar configurações ou serviços aqui se necessário
app.config(function ($httpProvider) {
    // Configurações globais para $http se necessário
});

app.run(function ($rootScope) {
    // Código para inicialização global se necessário
});

